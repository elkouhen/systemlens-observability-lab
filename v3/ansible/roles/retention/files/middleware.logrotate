# Troncature quotidienne sans archive : collecte Fleet avant suppression.
/var/log/mongodb/*.log /var/log/kafka/*.log /var/log/postgresql/*.log {
    daily
    rotate 0
    missingok
    notifempty
    copytruncate
    su root root
}
