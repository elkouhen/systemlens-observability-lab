{
  "description": "Expose le nom d'hote OTel au format attendu par les dashboards existants",
  "processors": [
    {
      "script": {
        "if": "ctx.resource != null && ctx.resource.attributes != null && ctx.resource.attributes.containsKey('host.name')",
        "lang": "painless",
        "source": "if (ctx.host == null) { ctx.host = new HashMap(); } ctx.host.name = ctx.resource.attributes['host.name'];"
      }
    }
  ]
}
